name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build macOS app
        run: |
          chmod +x apps/macos/build.sh
          apps/macos/build.sh
          rm -rf plugin/assets/Codex\ Usage\ Monitor.app
          mkdir -p plugin/assets
          cp -R apps/macos/build/Codex\ Usage\ Monitor.app plugin/assets/

      - name: Package plugin
        run: |
          mkdir -p dist
          cp apps/macos/build/Codex-Usage-Monitor-macOS.zip dist/Codex-Usage-Monitor-macOS.zip
          ditto -c -k --sequesterRsrc --keepParent plugin dist/codex-usage-monitor-plugin.zip

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if ! gh release view "${GITHUB_REF_NAME}" >/dev/null 2>&1; then
            gh release create "${GITHUB_REF_NAME}" --generate-notes --title "Codex Usage Monitor ${GITHUB_REF_NAME}"
          fi
          gh release upload "${GITHUB_REF_NAME}" dist/*.zip --clobber
